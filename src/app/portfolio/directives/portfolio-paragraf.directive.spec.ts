import { PortfolioParagrafDirective } from './portfolio-paragraf.directive';

describe('PortfolioParagrafDirective', () => {
  it('should create an instance', () => {
    const directive = new PortfolioParagrafDirective();
    expect(directive).toBeTruthy();
  });
});
