import { LangMarkupPipe } from './lang-markup.pipe';

describe('LangMarkupPipe', () => {
  it('create an instance', () => {
    const pipe = new LangMarkupPipe();
    expect(pipe).toBeTruthy();
  });
});
